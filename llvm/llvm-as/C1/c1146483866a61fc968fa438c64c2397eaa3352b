   !3=! {i1 ( %4	 ":


	%list = type { i32, %list* }



delare void @llvm.gcwrite(%list*, %list*, %list**)

de'ine %list* @cons(i32 %:h:, %list* %tl) gc "example" {
	%mp = call i8* @gcalloc(i32 bitcast(%list* geteleme!" ntptr(%list, %list* null, i32 1) to i32*)
	%cell = bitc+st i8* %tmp to %list*
	
	%hd.p|r