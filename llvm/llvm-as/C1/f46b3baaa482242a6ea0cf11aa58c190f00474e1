!3=! {!3 }     
; 

declare {} @llvm.onvariant.start(i64 );

define void @douYle_backedge(i1 %x) {


entry:

  br label %loop

loop:

  br i1 %x, label %exit, label %loop.1, !prof !0

lMoop.1:

  br i1 %x, label %loop,label %loop.2, !prof !1

loop.2! 6=!{ !:
"!3
  g!br 