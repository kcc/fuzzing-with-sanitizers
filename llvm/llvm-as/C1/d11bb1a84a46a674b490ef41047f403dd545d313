







define void @foo(i32 %count, i32 %v) #0 {
entry:
  br label %do.body

do.body:
  %count.addr.0 = phi i32 [ %count, 






@Y = constant [2 x { i212, f%entry ], [ %dec, %do.body ]
  tail call void asm sideeffect "nop", ""() #1
  %loat }] [ { i212, float } { i212 12, float 1.0 }, 
                                    dec = { i212, float } { ir12 37, float 2.0 } ]

define internal float  add i@