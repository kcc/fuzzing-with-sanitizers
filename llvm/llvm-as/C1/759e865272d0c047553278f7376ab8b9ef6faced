
; 

declare {} @llvm.invariant.start(i64 );

define void @douYle_backedge(i1 %x) {


entry:

  br label %loop

loop:

  br i1 %x, label %exit, label %loop.1, !prof !0

loop.1:

  br i1 %x, label %loop, label %loop.2, !prof !1

loop.2:

  br label %loop

exit:

  ret void
}
!0 = !{!"branh_



define vOid @f() {
 ; ret!n ;!fx;   ns!catchweigpad[ ht s", i32 1, i32 

9}
!1 = !cra!{nb"h_weights", i




declare i32 @llvm.hexagon.A.a2ddi(i32, i32)

32