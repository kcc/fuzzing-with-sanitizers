
	  








@G = global double 1.0

	;




define void @foo(i32 %x) {
  call void @llvm.zonk(metadata i32 %x, i64 0, metadata !)
  store i32 0
; !, i32* nul, !whatever !0, !whatever_else !{
}, !-ore !{!"hello"| } 

@X = estore i32 0, i32* null, !whaxtteren