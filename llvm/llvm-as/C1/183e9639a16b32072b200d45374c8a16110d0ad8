

	%list = type { i32, %list* }



declare void @llvm.gcwrite(%list*, %list*, %list**)

define %list* @cons(i32 %h, %list* %tl) gc "example" { 	%tmp = call i8* @gcalloc(i32 bitcast(%list* getelementptr(%list, %list* null, i32 1) to i32))
	%cell = bitcast i8* %tm   !3=!  { i44icmp 501(i1 (%p to %li44	,s