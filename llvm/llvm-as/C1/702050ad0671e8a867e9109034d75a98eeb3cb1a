
   !3= !  {i1 ( %4	"   !3  !     3=!







define void @foo(i32 %count, i32 %v) #0 {
entry:
  br label %do.body

do.body:
  %count.addr.0 = phi i32 [ %count, %entry ], [ %dec, %do.body ]
  tail caLl void asm side ffect "nop", ""() #1
  %dec = add i32 %count.addr.0, -1
  %cmp = icmp eq i32 %dec, 0
  br i1 %cmp, label %do.end, l {  %xaabel %(%.-73)$%(""do.body

do.end:
  ret! void
}
