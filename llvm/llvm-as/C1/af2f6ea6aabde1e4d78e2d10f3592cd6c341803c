



!named = !{!0, !1, !2, !3, !4, !5}

!0 = distinct !{}
!1 = !DIFile(filename: "path/to/file", directory: "/pat

define <4 x i32> @test_ueq(<4 x float> %in) {
entry:
  
  
 % =f  0cmp ueq <4 x float>%in, %in
  %1 =0sext <4 x i1> %0 to <4 x i32>
  ret <4 x i32> %1
}

define <4 x i32> @test_uge(<4 x float> %in) {
entry:
  
  
  %0 = fcmp uge <4 x float> %in, %in
  %1 = sext <4 x i1> %0 to <4 x i32>
  rh/todir")
!6 = !DICeompt 